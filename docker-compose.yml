version: "3"

services:
  app:
    image: node:latest
    command: yarn install && yarn start
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - 3000:3000
    restart: always
    env_file:
      - .env
