version: "3"

services:
  app:
    image: node:latest
    command: yarn start
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - ${PORT}:${PORT}
    restart: always
    env_file:
      - .env
