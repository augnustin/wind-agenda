import ReactDOM from "react-dom/client";
import * as bootstrap from "bootstrap";

import ConfigApp from "./components/ConfigApp";

// loadMap();

ReactDOM.createRoot(document.getElementById("configurator")).render(<ConfigApp />);
